{ //this file must be in the wwroot folder for WASM but not for server. WASM compiles then sends it over to client so it needs it.
  // "BaseAPIUrl": "https://localhost:44354/" doesnt work on azure
  "BaseAPIUrl": "https://vacationrental-api.azurewebsites.net/" 
}